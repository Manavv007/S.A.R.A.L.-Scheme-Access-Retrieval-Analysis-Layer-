# S.A.R.A.L. scraper dependencies (install in addition to backend/requirements.txt)
scrapy>=2.11
scrapy-playwright>=0.0.43
playwright>=1.40
w3lib>=2.1
PyYAML>=6.0
# Shared with the backend (embedding + vector store). Pinned loosely to match backend.
langchain-huggingface
langchain-pinecone
langchain-text-splitters
sentence-transformers>=2.7.0
python-dotenv
